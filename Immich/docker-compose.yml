#
# WARNING: To install Immich, follow our guide: https://immich.app/docs/install/docker-compose
#
# Make sure to use the docker-compose.yml of the current release:
#
# https://github.com/immich-app/immich/releases/latest/download/docker-compose.yml
#
# The compose file on main may not be compatible with the latest release.

name: immich

services:
  immich-server:
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}
    # extends:
    #   file: hwaccel.transcoding.yml
    #   service: cpu # set to one of [nvenc, quicksync, rkmpp, vaapi, vaapi-wsl] for accelerated transcoding
    volumes:
      # Do not edit the next line. If you want to change the media storage location on your system, edit the value of UPLOAD_LOCATION in the .env file
      - ${UPLOAD_LOCATION}:/data
      - ${FOTO_UPLOAD_LOCATION}:/Fotos
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - stack.env
    ports:
      - '2283:2283'
    depends_on:
      - redis
      - database
    restart: always
    healthcheck:
      disable: false
    labels:
      # Volg officiële numerieke versies én de 'release' tag. Digest tracking is cruciaal voor de 'release' tag.
      - 'wud.tag.include=^\d+\.\d+\.\d+$$'
      - 'wud.tag.include=release'
      - 'wud.watch.digest=true'

  immich-machine-learning:
    container_name: immich_machine_learning
    # For hardware acceleration, add one of -[armnn, cuda, rocm, openvino, rknn] to the image tag.
    # Example tag: ${IMMICH_VERSION:-release}-cuda
    image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}
    # extends: # uncomment this section for hardware acceleration - see https://immich.app/docs/features/ml-hardware-acceleration
    #   file: hwaccel.ml.yml
    #   service: cpu # set to one of [armnn, cuda, rocm, openvino, openvino-wsl, rknn] for accelerated inference - use the `-wsl` version for WSL2 where applicable
    volumes:
      - model-cache:/cache
    env_file:
      - .env
    restart: always
    healthcheck:
      disable: false
    labels:
      # Volg officiële numerieke versies én de 'release' tag. Digest tracking is cruciaal voor de 'release' tag.
      - 'wud.tag.include=^\d+\.\d+\.\d+$$'
      - 'wud.tag.include=release'
      - 'wud.watch.digest=true'

  redis:
    container_name: immich_redis
    # SHA-digest verwijderd om WUD in staat te stellen updates te zien voor de '8-bookworm' tag
    image: docker.io/valkey/valkey:8-bookworm
    healthcheck:
      test: redis-cli ping || exit 1
    restart: always
    labels:
      # Match de exacte tag en gebruik digest tracking om wijzigingen op de tag te detecteren.
      - 'wud.tag.include=^8-bookworm$'
      - 'wud.watch.digest=true'

  database:
    container_name: immich_postgres
    # SHA-digest verwijderd om WUD in staat te stellen updates te zien voor deze complexe tag
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: ${DB_DATABASE_NAME}
      POSTGRES_INITDB_ARGS: '--data-checksums'
      # Uncomment the DB_STORAGE_TYPE: 'HDD' var if your database isn't stored on SSDs
      # DB_STORAGE_TYPE: 'HDD'
    volumes:
      # Do not edit the next line. If you want to change the database storage location on your system, edit the value of DB_DATA_LOCATION in the .env file
      - ${DB_DATA_LOCATION}:/var/lib/postgresql/data
    shm_size: 128mb
    restart: always
    labels:
      # Match de start van de complexe tag en gebruik digest tracking.
      - 'wud.tag.include=^14-vectorchord'
      - 'wud.watch.digest=true'

volumes:
  model-cache:
